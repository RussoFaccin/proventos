import React from 'react';
// Chart
import { Line } from 'react-chartjs-2';

const AppChart = (props) => {
    const data = {};

    return (
        <Line data={data} />
    );
}

export default AppChart;